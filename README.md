# Lost Cities Online

Real-time online version of Lost Cities (based on rules.md).

## Quick start

Server:

```
cd server
npm install
npm run dev
```

Client:

```
cd client
npm install
npm run dev
```

Open `http://localhost:5173` and set server URL to `ws://localhost:8080`.

## Rules

See [rules.md](./rules.md).
